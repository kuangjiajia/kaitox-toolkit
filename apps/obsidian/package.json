{
  "name": "@kaitox/obsidian",
  "version": "0.3.0",
  "description": "Kaitox Obsidian plugin — Obsidian product of the Kaitox toolkit. Current feature: sync the active note as an X (Twitter) Article draft via the local relay and browser extension.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix ../.. && node esbuild.mjs",
    "build:only": "node esbuild.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@kaitox/relay-protocol": "^0.4.0",
    "@kaitox/x-article": "^0.4.0"
  },
  "devDependencies": {
    "obsidian": "^1.4.11"
  }
}
