# Tutorial images / 教程图片

Screenshots referenced by the main [README](../../README.md) / [README.zh-CN](../../README.zh-CN.md).

All four are in place and shown live in the READMEs. To replace one, overwrite the PNG here under the same file name — the README references stay stable.

主 [README](../../README.md) / [README.zh-CN](../../README.zh-CN.md) 引用的截图。四张都已就位并在 README 里直接展示。要换图，按同名覆盖本目录下的 PNG 即可——README 里的引用保持不变。

| File | What it shows |
|---|---|
| `01-load-extension.png` | `chrome://extensions` with Developer mode on, **Load unpacked** selecting the unzipped extension folder. |
| `02-panel.png` | The Kaitox panel in the corner of `x.com/compose/articles`. |
| `03-push.png` | The agent (Claude Code / Codex) syncing a Markdown file — running the `kaitox-x-article` skill and the local `kaitox` CLI. |
| `04-upload-result.png` | The Kaitox draft box (草稿箱) on `x.com` with a draft selected and the **上传草稿** button. |

Suggested: ~1600px wide, light background. Keep the file names stable so the README links don't drift.
